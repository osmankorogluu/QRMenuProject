@using QRMenuWebUI.Dtos.TestimonailoDtos
@model List<ResultTestimonialDto>

@{
    ViewData["Title"] = "Referanslar";
    Layout = "~/Views/AdminLayoutController1/Index.cshtml";
}

<style>
    body {
        background: #f4f6f9;
        font-family: 'Segoe UI', sans-serif;
        color: #2d2d2d;
    }

    .page-wrapper {
        max-width: 1100px;
        margin: 40px auto;
    }

    .card-modern {
        border: 1px solid #e5e7eb;
        border-radius: 12px;
        background: #fff;
        box-shadow: 0 2px 6px rgba(0,0,0,.05);
    }

    .card-header-modern {
        background: #fff;
        border-bottom: 1px solid #e5e7eb;
        font-weight: 600;
        font-size: 1rem;
        padding: 16px 20px;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .table-modern th {
        background-color: #fafafa !important;
        font-weight: 600;
        text-transform: uppercase;
        font-size: .8rem;
        color: #444;
    }

    .table-modern td {
        vertical-align: middle;
        font-size: .9rem;
    }

    .desc {
        max-width: 360px;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
    }

    .btn-sil {
        background: transparent;
        border: 1px solid #dc3545;
        color: #dc3545;
        padding: 4px 12px;
        border-radius: 6px;
        font-size: 0.85rem;
        transition: all 0.2s ease;
    }

        .btn-sil:hover {
            background: #dc3545;
            color: #fff;
        }

    .btn-guncelle {
        background: transparent;
        border: 1px solid #28a745;
        color: #28a745;
        padding: 4px 12px;
        border-radius: 6px;
        font-size: 0.85rem;
        transition: all 0.2s ease;
    }

        .btn-guncelle:hover {
            background: #28a745;
            color: #fff;
        }
</style>

<div class="content">
    <div class="page-wrapper">
        <div class="card-modern">
            <div class="card-header-modern">
                <span><i class="la la-users me-2"></i> Referans Yönetimi</span>
                <a href="/Testimonial/CreateTestimonial/" class="btn btn-dark btn-sm fw-bold">
                    <i class="la la-plus-circle me-1"></i> Yeni Referans
                </a>
            </div>

            <div class="card-body p-4">
                <table class="table table-modern table-hover text-center align-middle">
                    <thead>
                        <tr>
                            <th style="width:60px">#</th>
                            <th>Ad Soyad</th>
                            <th>Ünvan</th>
                            <th>Yorum</th>
                            <th>Görsel</th>
                            <th>Durum</th>
                            <th style="width:200px">İşlemler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null && Model.Any())
                        {
                            var i = 1;
                            foreach (var item in Model)
                            {
                                <tr>
                                    <td>@i</td>
                                    <td>@item.Name</td>
                                    <td>@item.Title</td>
                                    <td class="text-start">
                                        <span class="desc" title="@item.Comment">@item.Comment</span>
                                    </td>
                                    <td>
                                        @if (!string.IsNullOrWhiteSpace(item.ImageUrl))
                                        {
                                            <img src="@item.ImageUrl" alt="@item.Name" width="40" height="40" class="rounded-circle" />
                                        }
                                        else
                                        {
                                            <span class="text-muted">—</span>
                                        }
                                    </td>
                                    <td>
                                        @if (item.Status)
                                        {
                                            <span class="badge bg-success">Aktif</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-secondary">Pasif</span>
                                        }
                                    </td>
                                    <td>
                                        <a href="/Testimonial/UpdateTestimonial/@item.TestimonialID"
                                           class="btn-guncelle me-2">
                                            Güncelle
                                        </a>
                                        <a href="/Testimonial/DeleteTestimonial/@item.TestimonialID"
                                           class="btn-sil">
                                            Sil
                                        </a>
                                    </td>
                                </tr>
                                i++;
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="7" class="text-muted py-4">
                                    <i class="la la-inbox fa-2x d-block mb-2"></i>
                                    Hiç referans kaydı bulunamadı.
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
